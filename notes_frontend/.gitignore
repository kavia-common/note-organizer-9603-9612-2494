# Flutter/Dart/Pub
.dart_tool/
.packages
.pub/
build/

# Platform
ios/Flutter/
ios/Pods/
android/.gradle/
android/local.properties

# IDE
.idea/
.vscode/
*.iml

# Override root .gitignore: .env is a required Flutter asset declared in pubspec.yaml.
# Without this file, `flutter build` fails with "Failed to bundle asset files".
!.env
