# Environment configuration for Notes Frontend
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
#
# This file is referenced as a Flutter asset in pubspec.yaml.
# The .env file MUST exist for the Flutter build to succeed.

# Port for the web server (used by the preview/dev environment)
PORT=3001
